  var n = 2
    var i = 0
    val primeArray = IntArray(50)
    while (i < 50) {
        if (isPrime(n)) {
            primeArray[i] = n
            i += 1
            n += 1
        }
        else {
            n += 1
        }

------------------------------------


  fun isPrime(n: Int): Boolean{
      for(i in 2 until n){
          if(n%i==0)return false
      }
      return true
  }

